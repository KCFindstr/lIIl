rm cpu.
del cpu.

v-- mian is application node
mian << OvO { <-- mian is application node
^-- mian is application node
  "Hello world: " >> op@cpu.
  OvO >> op@cpu.
  "\n" >> op@cpu.
  (OvO + 1)>> mian.
}

0 >> mian.
